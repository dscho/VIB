TODO for the ImageJ 3D Viewer
===========================================================

* Catch exception when dublicate content name
* Automatically update name when selection a image to add
* load images/textures/surfaces in an extra thread
* bug when renaming a point
* slices: x/y/z + scroll
* possibility to scale a point
* scale point up for highlighting
* After registering, show points of template and model
* remove extra panel after registration
* lock template and model after registration
* run the slide update methods in the menubar in an extra thread
* Show "Adjust slices" in registration mode
